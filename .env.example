OPENAI_API_KEY="<replace-with-your-openai-api-key>"
REACT_APP_BACKEND_URL=http://localhost:8000 # This is the URL of the backend server and may need to be changed if you are running the backend server on a different port or on a different machine.